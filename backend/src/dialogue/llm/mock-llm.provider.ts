/**
 * @deprecated TB08-4 起 Mock provider 已迁移至 ./providers/mock.provider。
 * 本文件仅作兼容性再导出，避免重复维护两份逻辑。新代码请直接引用 providers/mock.provider。
 */
export { MockLlmProvider, KNOWLEDGE_PREFIX } from './providers/mock.provider';
