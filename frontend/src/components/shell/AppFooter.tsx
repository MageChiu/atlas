/** F1.2 - 页尾 */
export function AppFooter() {
  return (
    <footer className="border-t border-atlas-border bg-atlas-surface/60 px-4 py-2 text-center text-xs text-atlas-muted">
      Atlas · 路过 — 在线互动式虚拟旅行体验 (MVP)
    </footer>
  );
}
