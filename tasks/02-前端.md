# 02 - 前端轨道

> **轨道：前端** ｜ **阶段：B（公共部分完成后并行）** ｜ **依赖：[00-公共部分.md](./00-公共部分.md) 冻结**
>
> 技术栈：React / Next.js + PixiJS + GSAP + Zustand + TanStack Query + Tailwind + TypeScript。
> 与 [01-后端.md](./01-后端.md) **并行推进**，优先基于 C5 Mock 开发，阶段 C 切换真实接口联调。
> 分层：App Shell 层 / Scene Runtime 层 / Domain UI 层。

---

## F0. 初始化（M1）

- [x] **F0.1** 初始化 Next.js + TypeScript 工程，引用 `shared/` 共享类型。
- [x] **F0.2** 接入 Tailwind，建立 C1.2 规范。
- [x] **F0.3** 搭建目录结构：`app/ components/ scenes/{world,region,spot,event} stores/ services/ hooks/ types/ utils/ assets/`。
- [x] **F0.4** 搭建 Zustand stores：`authStore`、`sceneStore`、`progressStore`、`actionStore`、`uiStore`。
- [x] **F0.5** 搭建 API 请求层（TanStack Query）+ Mock 服务（基于 C5 数据）。

**验收**：工程本地可运行，Mock 可返回 world/region/spot 基础数据。

---

## F1. App Shell 层（M1~M2，P0）

- [x] **F1.1** 登录态管理与路由：`/`、`/world`、`/region/[id]`、`/spot/[id]`、`/collections`、`/generated`、`/share/[id]`。
- [x] **F1.2** 页头页尾、全局弹窗与通知容器。
- [x] **F1.3** 全局请求封装、错误码统一处理（依据 C4）。

**验收**：路由可达，登录态与全局 UI 框架就位。

---

## F2. 地图与场景骨架（M2，P0）

- [x] **F2.1** `WorldScene`：展示世界概貌与开放区域，点击进入 Region。
- [x] **F2.2** `RegionScene`：展示 Spot 节点、路径/解锁状态，点击进入 Spot。
- [x] **F2.3** `SpotScene`：展示景点主视觉，渲染 Action 入口。
- [x] **F2.4** `EventLayer`：承接 Action 结果，展示后回到 Spot Scene。
- [x] **F2.5** PixiJS 容器渲染树管理；所有节点从接口/Mock 配置读取（不写死）。

**验收**：World→Region→Spot 层级可跑通（Mock 驱动）。

---

## F3. 相机与转场（M2，P0）

- [x] **F3.1** Camera 抽象：`zoomTo / panTo / focusOn / transitionTo / resetView`。
- [x] **F3.2** sceneStore 管理 `currentLevel / currentRegionId / currentSpotId / cameraState / transitionState`。
- [x] **F3.3** 进入/回退使用统一过渡动画（GSAP），回退为进入的镜像逻辑。
- [x] **F3.4** 相机参数从配置读取，不写死在组件；不用页面跳转替代层级转场。

**验收**：层级切换有统一转场动画，相机由 Scene Runtime 统一管理。

---

## F4. Action / Event（M3，P0）

- [x] **F4.1** Action 面板：统一结构（icon/title/subtitle/status/handler），所有类型经统一渲染入口。
- [x] **F4.2** Action 执行流程：点击 → 调 `execute` → loading → 接收结果 → 渲染 Event/Reward/AI 状态。
- [x] **F4.3** actionStore 管理 `availableActions / executingActionId / lastActionResult / cooldownMap`。
- [x] **F4.4** 接入 `check_in`、`random_event`、`encounter` Action。
- [x] **F4.5** 展示组件：`EventModal`、`RewardPopup`、`EncounterPanel`。

**验收**：Spot 内执行至少 2 类 Action，能展示 Event 与 Reward（Mock 或真实）。

---

## F5. AI 图片链路（M4，P0 入口 → P1 完整）

- [x] **F5.1** `PhotoGeneratePanel`：photo Action 入口。
- [x] **F5.2** 图片上传组件（调 `uploads/image`）。
- [x] **F5.3** 生成任务提交（调 `ai/photo-generate`）。
- [x] **F5.4** 任务轮询（`pollTaskStatus`）。
- [x] **F5.5** 结果页展示（`GeneratedPhotoPreview`）。

**验收**：上传 → 提交 → 轮询 → 结果展示链路跑通（结果可为 Mock）。

---

## F6. 用户资产与分享（M5，P1）

- [x] **F6.1** 图鉴页 `/collections`（调 `me/collections`）。
- [x] **F6.2** 生成记录页 `/generated`（调 `me/generated-assets`）。
- [x] **F6.3** 分享页 `/share/[id]` 与结果海报页。

**验收**：用户可在图鉴/记录/分享页看到结果沉淀。

---

## F7. 联调收口（M6，阶段 C）

- [ ] **F7.1** Mock 切换真实接口，对齐 world/region/spot、actions、execute-action 结构。
- [ ] **F7.2** AI 任务状态联调、错误处理与回退机制。
- [ ] **F7.3** 动画统一、埋点补齐、性能优化（P2）。

**前端交付物**：工程目录结构、页面与场景骨架、状态管理定义、关键组件、联调说明。
