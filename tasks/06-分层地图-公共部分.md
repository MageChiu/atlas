# 06 - 分层地图底图 · 公共部分（@atlas/shared）—— 必须先完成

> 交付给**公共 Agent**，无需阅读其他任务文件即可执行。
> 上位设计：`tasks/06-分层地图底图.md`（方案 A：地球作为根 Region）。
> 完成并 `npm run build:shared` 通过后，**解锁前端与素材并行**。
>
> 约束（`docs/Atlas-路过-协作开发规范.md`）：类型/枚举/seed 的唯一事实来源是 `@atlas/shared`；前后端只读引用。

---

## 0. 已锁定前提
- 地球是 DAG 根 Region `region_earth`（`type: 'world'`），`worldRootRegionIds = ['region_earth']`。
- 子节点用自身 `Region.geo`（中心经纬度）投影到**父**底图，复用 03 的 `geoToPixel`。
- 容器层用线性投影（低精度展示，已接受）；世界层用等距圆柱 bbox `{-180,180,-90,90}`、底图 2:1。

## 1. 任务

### TC06-1 RegionType 增 World（`shared/src/enums.ts`）
- [x] `RegionType` 增 `World: 'world'`（根/世界层）。其余值不变。

### TC06-2 Region 增中心经纬度（`shared/src/models/content.ts`）
- [x] `Region` 增可选字段 `geo?: GeoPoint`（区域中心/代表点，WGS84；用于在父地图上投影定位本区域节点）。
- [x] 复用现有 `GeoPoint`（已定义），不要新增等价类型。

### TC06-3 新增根 Region region_earth + 容器层补底图元数据与 geo（`shared/src/seed/dataset.ts`）
- [x] 新增常量 `REGION_EARTH = 'region_earth'`，并置于 regions 数组首位（type=world，全球 bbox 2:1，children=[亚洲]）。
- [x] `worldRootRegionIds` 改为 `[REGION_EARTH]`。
- [x] 为 `region_asia` / `region_china` / `region_sichuan` 补 `geoBounds`+`mapSize`+`geo`（mapSize 待素材回填真实像素）。
- [x] 为 `region_chengdu` 补 `geo: { lng: 104.07, lat: 30.66 }`。
- [x] `region_qingcheng_dujiangyan_heritage` 补 `geo ≈ {lng:103.57,lat:30.95}`。
- [x] 各层 mapSize 宽高比经 `assertBoundsRatio` 校验通过。

### TC06-4 验证
- [x] `npm run build:shared` 通过。
- [x] 自检：earth/asia/china/sichuan/chengdu 调 `assertBoundsRatio` 全部 `ok=true`（最大偏差 0.07%）。
- [x] `worldRootRegionIds` 返回 `['region_earth']`；`region_earth.children` 指向 `region_asia`。

## 2. 出口
- 构建 + 比例校验通过 → **解锁** `06-分层地图-前端.md`、`06-分层地图-素材.md`。
- 同步 `docs/Atlas-路过-数据模型设计.md`（Region.geo 字段、region_earth 根节点、world 类型）。

## 3. 注意
- 容器层 mapSize 为**估值**，素材任务（TS06）产出真实底图后需回填真实像素并重过校验——在素材任务中标注回填点。
- 后端无需改动：`getWorld` 已按 `worldRootRegionIds` 返回（04 已实现），现自动返回 `region_earth`。
